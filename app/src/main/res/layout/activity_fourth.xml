<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fourth_layout"
    tools:context=".activity.FourthActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar_layout" />

    <fragment
        android:id="@+id/fragment_chooser"
        android:name="com.keepsolid.gittestapp.fragment.ChooserFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/default_middle_margin"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/container" />

    <FrameLayout
        android:id="@+id/container"
        app:layout_constraintTop_toBottomOf="@+id/fragment_chooser"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="0px"
        android:layout_height="0dp" />


</androidx.constraintlayout.widget.ConstraintLayout>